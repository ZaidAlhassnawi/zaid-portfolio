# Security Model

هذا المشروع Frontend ثابت؛ لذلك لا توجد أسرار آمنة يمكن تخزينها داخله. أي قيمة داخل ملفات JavaScript يستطيع الزائر رؤيتها.

## ما يجوز وضعه في `config.js`

- Supabase Project URL.
- Supabase Publishable key أو anon key.
- اسم Storage bucket.

## ما يُمنع وضعه

- Service role key.
- PostgreSQL password.
- Database connection string.
- كلمات مرور المستخدمين.
- JWT secret.

## طبقات الأمان

1. **Authentication:** الدخول بواسطة Supabase Auth، مع تعطيل التسجيل العام.
2. **Authorization:** سياسات Row Level Security تحدد من يستطيع القراءة أو التعديل.
3. **Storage policies:** القراءة عامة للملفات المنشورة، والرفع والتعديل والحذف للمالك داخل مجلده فقط.
4. **Browser validation:** التحقق من الحقول والملفات لتحسين الاستخدام، لكنه لا يستبدل سياسات الخادم.
5. **Output escaping:** البيانات النصية القادمة من قاعدة البيانات تُعرض بعد escaping لتقليل مخاطر XSS.

## لماذا المفتاح العام آمن للنشر؟

المفتاح العام يعرّف مشروع Supabase ولا يمنح صلاحيات المدير. الصلاحيات النهائية يحددها JWT الخاص بالجلسة وسياسات RLS. يصبح المشروع غير آمن فقط عندما تكون سياسات RLS مفقودة أو واسعة أكثر من المطلوب.

## فحص دوري

- راجع قائمة مستخدمي Auth واحذف أي حساب غير معروف.
- راجع RLS بعد إضافة أي جدول جديد.
- راجع Storage policies بعد إنشاء bucket جديد.
- لا تسجل كلمات المرور أو session tokens في console.
- غيّر كلمة مرور المدير عند الاشتباه، ثم سجّل الخروج من جميع الجلسات.
